# Subly — 整体技术方案

> 定位：你的自托管续费雷达，集中管理订阅、域名、VPS、保号套餐与提醒通道；内置 SQLite 零配置，中文单语言，支持多用户、注册审核、多货币、多主题、Telegram + Bark + Webhook 多通道提醒、内置服务管理、备份恢复与实时日志。

---

## 1. 总体架构

```text
┌────────────────────────────────────────────────────────────┐
│                         浏览器 Web UI                       │
│ Vue 3 · Vite · Pinia · Vue Router · vue-i18n（仅中文）       │
│ 雷达工作台 · 订阅账本 · 续费日历 · 财务报表 · 管理控制台       │
└──────────────────────────────┬─────────────────────────────┘
                               │ HTTPS / REST API
┌──────────────────────────────▼─────────────────────────────┐
│                        FastAPI 后端                         │
│ JWT 鉴权 · 用户审核 · 订阅 CRUD · 报表统计 · 备份恢复         │
│ 设置管理 · 通知日志 · 实时活动日志 · 静态前端托管             │
├──────────────┬────────────────┬────────────────┬───────────┤
│ APScheduler  │ 通知服务        │ 汇率服务        │ 图标服务  │
│ 到期扫描      │ TG+Bark+Webhook │ 汇率拉取/缓存   │ 上传/缓存 │
└──────┬───────┴────────┬───────┴────────┬───────┴───────────┘
       │                │                │
┌──────▼──────┐   ┌─────▼─────┐    ┌─────▼─────┐
│ SQLite       │   │ 通知目标  │    │ 汇率 API   │
│ /app/data    │   │ 外部推送  │    │ 第三方数据 │
└──────────────┘   └───────────┘    └───────────┘
```

部署形态：

- 单镜像同时托管 FastAPI API、上传图标静态资源与前端 SPA。
- Dockerfile 使用 `node:20-alpine` 构建前端，再用 `python:3.12-slim` 运行后端。
- 容器内服务监听 `0.0.0.0:8000`，NAS / Docker Hub compose 默认映射到宿主机 `8842`。
- 源码部署的 `docker-compose.yml` 额外使用 Caddy，在 `80/443` 反代到 `app:8000`。
- 持久化目录为 `/app/data`，保存 SQLite 数据库、上传图标和内置图标库缓存。

---

## 2. 技术选型

| 层 | 当前选型 | 说明 |
|----|----------|------|
| 后端 | Python 3.12 + FastAPI | REST API、依赖注入、后台任务清晰 |
| ORM / 数据库 | SQLAlchemy + SQLite | 零配置，数据库文件持久化在 `/app/data/subly.db` |
| 配置 | pydantic-settings | 通过 `.env` / 环境变量读取部署配置，忽略未知变量 |
| 迁移 | 轻量在线迁移脚本 `migrate.py` | 启动时按列补齐旧库；必需 DDL 失败即终止启动，历史数据清理仍按兼容策略记录告警 |
| 调度 | APScheduler | 每日按 `REMINDER_SCAN_TIME` 扫描到期订阅 |
| 通知 | Telegram Bot API + Bark API + HMAC Webhook | 三个通道可同时启用、独立发送、独立记录日志 |
| 前端 | Vue 3 + Vite + Pinia + Vue Router | 单页应用，构建后由后端静态托管；路由标题 / 主标题焦点、原生表单语义与移动抽屉焦点约束统一管理 |
| 文案 | vue-i18n（仅中文 `zh`） | 保留集中式文案管理，不再维护英文 / 俄文语言包 |
| 可访问交互 | `AppModal` + `useDialogFocus` + `useBodyLock` + `useConfirm` | Dialog 栈只处理顶层 Escape / Tab，pending 时禁止关闭；移动抽屉复用同一焦点环绕并以 `inert` 隔离背景；Toast 按成功 / 错误分别使用 status / alert |
| 数据可视化 | `categoryVisual.js` + `trend.js` + 轻量 HTML/CSS 图表 | 分类颜色跟随分类 ID / 持久颜色而非排名；趋势 view model 统一轴、Tooltip、ARIA 与数据表，不引入大型图表库 |
| 主题启动 | 同源 `/theme-bootstrap.js` + 服务端权威用户主题 | Vue 挂载前从非敏感本地镜像应用白名单主题，`/api/auth/me` 成功后校正；SPA CSP 保持 `script-src 'self'` 且不开放 inline style |
| 部署 | Docker 多阶段构建 + 可选 Caddy | 支持 NAS、普通 Docker、源码 compose 与镜像 compose |

---

## 3. 配置与环境变量

后端配置源为 `backend/app/config.py` 中的 `Settings`。关键变量：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DB_PATH` | `data/subly.db` | SQLite 数据库文件路径；容器内建议保持默认并持久化 `/app/data` |
| `JWT_SECRET` | `change-me`（不安全，启动拒绝） | 登录令牌密钥；必须使用至少 32 字符随机强密钥 |
| `ALLOW_INSECURE_DEFAULTS` | `false` | 仅本地演示可显式跳过危险默认值保护，公网禁止开启 |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | `60` | Access Token 有效期 |
| `REFRESH_TOKEN_EXPIRE_DAYS` | `14` | Refresh Token 有效期 |
| `AUTH_COOKIE_NAME` | `subly_refresh` | HttpOnly Refresh Cookie 名称 |
| `AUTH_COOKIE_SECURE` | `false` | HTTPS 部署设为 `true`；直接 HTTP 局域网访问保持 `false` |
| `AUTH_COOKIE_SAMESITE` | `lax` | Refresh Cookie SameSite 策略（`lax` / `strict` / `none`）；`none` 必须同时启用 Secure，否则拒绝启动 |
| `TZ` | `Asia/Shanghai` | 应用时区 |
| `LOG_LEVEL` | `INFO` | 后端 stdout 日志级别 |
| `SLOW_REQUEST_MS` | `1000` | 超过该毫秒数的请求额外记录 `slow_request` |
| `APP_PUBLIC_URL` | 空 | 对外可访问地址（如 `https://subly.example.com`）；用于生成公网日历服务可订阅的私有 iCal URL、Bark 测试推送跳转，以及把上传/内置订阅图标转换为设备可访问的绝对 URL；真实提醒点击地址仍取订阅 `url` |
| `REMINDER_SCAN_TIME` | `09:00` | 每天规划到期订阅并写入可靠投递队列的时间 |
| `REQUIRE_ADMIN_APPROVAL` | `true` | 新用户注册后是否需要管理员审核 |
| `SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASSWORD` / `SMTP_FROM` / `SMTP_TLS` | 空 / `587` / `true` | SMTP 邮箱验证码配置 |
| `ADMIN_USERNAME` / `ADMIN_PASSWORD` / `ADMIN_EMAIL` | `admin` / `admin123`（首次创建时拒绝） / `admin@example.com` | 管理员账号；仅首次创建时要求密码至少 12 位且非默认值，已有管理员不会被环境变量重置 |
| `TELEGRAM_BOT_TOKEN` | 空 | 仅声明保留，当前不参与发送或验证；Telegram Bot Token 以用户级网页设置为准 |
| `EXCHANGE_API_BASE` / `EXCHANGE_API_URL` / `EXCHANGE_API_KEY` | `USD` / `https://open.er-api.com/v6/latest/` / 空 | 汇率数据源配置 |
| `ICON_FETCH_ENABLED` | `true` | 是否允许内置图标库联网下载 favicon |
| `ICON_FETCH_GOOGLE_ENABLED` | `true` | 是否启用 Google favicon provider |
| `ICON_FETCH_TIMEOUT_S` | `2.0` | 单次图标下载超时秒数 |
| `ICON_FETCH_MAX_BYTES` | `262144` | 单个图标最大下载字节数 |
| `ICON_FETCH_CONCURRENCY` | `6` | 冷缓存时最多并发下载 favicon 数量 |
| `ICON_FETCH_SVG_ENABLED` | `true` | 是否接受并消毒缓存远端 SVG favicon |

Bark 的 Device Key、服务器、提示音、分组与 TTL 均在网页「设置」中按用户配置。`APP_PUBLIC_URL` 用于测试推送点击跳转，以及把上传/内置订阅图标的站内相对路径转换为 Bark 设备可下载的绝对 URL；真实续费提醒点击地址始终取订阅自身 `url`。

---

## 4. 外部 API 与网络访问

### 4.1 Telegram Bot API

- 通过 `@BotFather` 创建 Bot 并获取 Bot Token。
- 用户需先与机器人对话，再在网页设置中获取或填写 Chat ID。
- 关键接口：
  - `getMe`：验证 Bot Token。
  - `getUpdates`：辅助获取 Chat ID。
  - `sendMessage`：发送续费提醒。
- 用户级设置支持 Telegram API 反代与 HTTP 代理，适配不同网络环境。

### 4.2 Bark API

- 用户在 iOS Bark App 中复制 Device Key，填写到网页「设置 → Bark 配置」。
- 支持自建 Bark 服务器地址。
- 支持提示音、分组与可选 TTL：
  - 留空：不传 `ttl` 字段，使用 Bark 默认行为。
  - `0` 或其他非负整数：测试推送与定时提醒都会显式透传给 Bark。
  - 前端与后端都拒绝负数、小数、指数形式等非非负整数输入。
- iOS 15+ 的真实续费提醒可携带订阅图片图标：绝对 HTTP(S) 图标直接透传；`/static/icons/` 与 `/api/icons/library/` 站内路径经 `APP_PUBLIC_URL` 转成绝对 URL。Emoji、普通文本、非法协议或无法绝对化的路径会被省略，不阻断提醒。
- Bark 图标 URL 的解析仅做 fail-closed 字符串校验：外部绝对图标与上传图标不会因此新增 Subly 后端抓取；内置图标冷缓存仍复用既有、带 SSRF 防护的 favicon 下载流程。完整 URL 会发送给 Bark 服务和接收设备，不应包含账号密码、临时签名或其他敏感信息。
- Bark 测试推送没有订阅上下文，继续不发送订阅图标；管理员 dry-run 仍只预览触发结果和文案，不模拟完整 transport payload。

### 4.3 汇率 API

- 默认使用 `https://open.er-api.com/v6/latest/`。
- 支持可选 `EXCHANGE_API_KEY`。
- 后端缓存汇率，订阅金额统一换算到用户的基准货币。
- `convert_strict()` 是财务与 UI 的严格换算入口：缺少、越权或非法汇率时返回 `None`，调用方以空折合值和完整性元数据显式暴露缺口，绝不把原币金额伪装成基准币；旧 `convert()` 仅保留兼容回退，财务/UI 调用方不得使用。
- 刷新触发：应用时区每天 `04:00` 自动刷新；缓存超过 12 小时且打开相关页面时自动刷新；管理员可手动调用 `/api/currencies/rates/refresh`。
- 用户可维护自定义货币与自定义显示符号。

### 4.4 私有 iCal Feed

- 管理接口：`GET /api/calendar-feed/status`、`POST /api/calendar-feed/generate`、`POST /api/calendar-feed/reset`、`DELETE /api/calendar-feed`；公共订阅入口为 `GET /api/calendar-feed.ics?token=...`。
- 每用户一条 `calendar_feed_tokens` 记录；原始 Token 使用 32 字节随机数生成，只在生成/重置响应显示一次，数据库只保存 SHA-256。重置通过 compare-and-set 避免并发响应拿到已失效链接，撤销保留记录并立即使链接失效。
- 缺失、非法、未知、旧或已撤销 Token，以及用户禁用、邮箱未验证或未审核，统一返回相同 404，避免暴露账户与 Token 状态。
- Feed 使用 `icalendar` 库生成 RFC 5545 内容，复用 `billing.add_cycle()` 与包含式 `end_date` 判断；窗口为业务今天前 31 天至未来 24 个月，只包含 recurring、active、未暂停且 `show_in_calendar=true` 的 occurrence。
- 每个 occurrence 是全天事件，`DTEND` 为次日排他日期；UID 由首次生成 Feed 时持久化的随机 namespace、用户 ID、订阅 ID 和日期构成，跨实例不冲突，修改公开地址或重置 Feed Token 都不改变 UID；不生成 VALARM，不输出 notes、IP、family members 或通知凭据。
- 管理响应与 Feed 统一 `private, no-store`、`noindex`、`no-referrer`。默认 Uvicorn access log 被关闭，应用请求日志只记录 path，不记录 query 中的 Token。

### 4.5 favicon 下载与缓存

内置服务库的图标按需下载并缓存：

1. 优先访问目标站点 `https://<domain>/favicon.ico`。
2. 解析站点首页的 `rel=icon` / `apple-touch-icon` 等链接。
3. 依次尝试公共 favicon provider：unavatar、iconhorse、simple-icons、jsDelivr simple-icons、DuckDuckGo。
4. 可选尝试 Google favicon provider（`ICON_FETCH_GOOGLE_ENABLED`）。
5. 全部失败时返回后端生成的可见 SVG fallback（稳定颜色 + 首字母），避免透明空白图标。

安全与稳定性约束：

- 允许缓存 PNG / ICO / WEBP / JPEG。
- 允许缓存远端 SVG，但必须先消毒：移除脚本、事件属性、外链、`data:` URL 等风险内容；可用 `ICON_FETCH_SVG_ENABLED=false` 禁用。
- 限制单次下载超时、最大字节数与冷缓存并发数。
- 保留单 slug 失败缓存、全局熔断和并发信号量，避免冷缓存请求风暴。
- 失败时提供可见 fallback；限流 fallback 使用 `no-store`，避免浏览器长期缓存临时失败结果。

---

## 5. 数据模型（核心表）

以下以 `backend/app/models.py` 为准，只列核心字段与用途。

```text
users
  id, username, email, password_hash
  is_admin, is_active
  email_verified, is_approved, email_code, email_code_expires
  locale, theme, base_currency, monthly_budget, category_order
  telegram_enabled, telegram_bot_token, telegram_chat_id,
  telegram_admin_id, telegram_api_base, telegram_proxy
  bark_enabled, bark_device_key, bark_server, bark_sound,
  bark_group, bark_ttl
  webhook_enabled, webhook_url, webhook_secret
  created_at

categories
  id, user_id?, name, icon, color, is_system, sort

payment_methods
  id, user_id?, name, icon, is_system

icon_library_services
  id, name, domain, website
  category, category_keys
  slug, is_active, sort, source
  created_at, updated_at

currencies
  code, name, symbol, is_custom, user_id?

exchange_rates
  id, base, quote, rate, updated_at

subscriptions
  id, user_id
  name, plan, icon, url, notes, remark, ipv4, ipv6
  category_id, payment_method_id, bundle_id
  amount, currency
  billing_type, is_keepalive, cycle, cycle_count
  start_date, next_renewal_date, end_date, last_renewed_at
  is_active, is_paused, auto_renew, show_in_calendar, sort
  family_members
  remind_days_before
  created_at

bundles
  id, user_id, name, note, created_at

activity_log
  id, user_id?, username?, level, action, detail, created_at

calendar_feed_tokens
  id, user_id (unique), token_hash (SHA-256, unique)
  uid_namespace (随机、唯一、持久化)
  created_at, updated_at, revoked_at?

notification_outbox
  id, delivery_id (随机 128-bit，unique), subscription_id, user_id
  business_date, days_before, channel
  status, subscription_name, renewal_date, payload
  retry_cycle, attempt_count, next_attempt_at?, lease_expires_at?, lease_token?
  last_error?, created_at, updated_at, sent_at?, canceled_at?
  UNIQUE(subscription_id, business_date, days_before, channel)

scheduler_state
  key, last_completed_business_date?, updated_at

notification_log
  id, subscription_id, user_id, outbox_id?, retry_cycle?, attempt_no?
  days_before, channel, status, message, sent_at

renewal_history
  id, subscription_id (ON DELETE CASCADE), user_id, renewed_at, mode,
  prev_renewal_date?, next_renewal_date?, amount, currency, created_at

imap_accounts
  id, user_id, email, password (只写不回显), provider ('126' | 'qq')
  banks (JSON；NULL/空数组 = 全部银行，非空数组 = 银行 key 白名单，
        key 见 app/bank_senders.py，与前端 creditCardBanks.js 一致)
  created_at
  UNIQUE(user_id, email)

credit_card_statements
  id, user_id, card_id? (nullable=未匹配), source_account_id
  bank_key, card_last_four, match_status ('matched'|'unmatched'|'ambiguous')
  bill_period_start?, bill_period_end?, statement_date?, due_date?
  total_due?, min_due?, credit_limit?, message_id (去重), subject?,
  verify_status ('ok'|'mismatch'，勾稽失败响亮标注),
  is_repaid (用户手动标记已还款；重新解析同一封邮件不覆盖),
  repaid_at?
  UNIQUE(source_account_id, message_id, card_last_four)

credit_card_statement_items
  id, statement_id, line_no (账单内行序), trans_date_raw, trans_date?, posted_date?
  description, amount (结算币), tx_amount?, tx_currency?, tx_type, installment_note?
  无行级 UNIQUE：同日同商户同金额的合法重复交易可共存；
  去重由邮件级 (source_account_id, message_id, card_last_four) 承担
```

要点：

- `users.locale` 是历史兼容字段，当前界面固定中文，不再暴露给前端用户设置。
- 用户审核由 `email_verified` 与 `is_approved` 表达；是否需要审核由 `REQUIRE_ADMIN_APPROVAL` 控制。
- Telegram / Bark / Webhook 是用户级设置，可按账号独立启用；Webhook secret 仅用于 HMAC-SHA256 签名，不进入事件 payload 或通知日志。
- `categories`、`payment_methods` 同时支持系统预置与用户自定义。
- `imap_accounts.banks` 是账单银行白名单：拉取时按邮件声明的 `From` 域名过滤（主域与子域后缀匹配，见 `app/bank_senders.py`），`GET /api/imap/accounts/banks` 提供可选银行清单。`From` 头可伪造，该过滤仅用于分类筛选，不构成发件人身份认证；未来账单解析若触发自动记账等可信动作，需结合 DKIM/SPF/DMARC 等认证结果。
- 账单解析（`app/services/credit_card_statement_parser.py`，标准库零依赖）在手动点击「解析账单」或账单日次日 23:50 起的限时自动调度（最多连续 3 天）时读取邮件正文；解析结果只进展示与备份 v4，不进通知 / iCal / 任何外部输出。金额勾稽自校验逐卡核对交易合计与账单明示总额，失败标 `mismatch` 响亮暴露。`From` 可伪造，解析结果不构成银行身份认证，不做自动记账。
- 待还款总额（`GET /api/credit-cards/outstanding/summary`）：所有已出账单未标记还款的 `total_due` 合计（含已删卡孤立账单；`mismatch` 与 `total_due` 为 NULL 的不计入）。`per_card[].cycles` 是各未还账单的月份标签（「26年8月」，取 `bill_period_end || statement_date` 的出账月，前端据此按月份命名账单），`overdue_cycles`/`max_overdue_days`/顶层 `overdue_total` 表达逾期——账单逾期是派生值（`_statement_out.is_overdue`：未标记 + `due_date` 已过，`due_date` NULL 不判逾期），不落库、标记后自动消除；逾期只做展示与标记交互，不改变提醒发送节奏。标记入口两处——卡片按钮 `POST /{card_id}/mark-repaid` 一次标记该卡全部未还账单（含历史各期），账单明细区 `PATCH /statements/{id}/repaid` 单期标记/取消；标记后前端实时刷新汇总。`is_repaid` 是用户手动标记，重复解析同一封邮件刷新账单字段时不覆盖；随备份 v4 导出/恢复。
- 已还款顺延与当期提醒静默：`credit_cards.repaid_through_due`（名义还款日，含界线）记录「已还到的期次」，标记还款时单调推进（取消标记不回拨）。还款日派生统一走 `credit_card_rules.next_due_date_after`：卡片展示（`_to_out` 的 next_due_date/days_until_due 及随动字段）、提醒扫描（`plan_reminder_candidates`）、投递前复核（`_prepare_delivery`——已入队的当期 pending 据此自动 canceled）三处同源；iCal（`calendar_feed`）与站内日历（前端 `creditCardCalendarEvents`）过滤 `occurrence <= repaid_through_due` 的期次。免息期是「今天消费」口径，与还款状态正交，不顺延。账单 `due_date` 为 NULL 时单期标记保守不推进界线。
- `icon_library_services.category` 是兼容主分类字段，`category_keys` 是 JSON 多分类数组；列表第一项作为主分类。
- `subscriptions.billing_type` 区分 `recurring` 周期订阅与 `one_time` 一次性买断。
- `subscriptions.is_keepalive` 保号套餐标记（短信保号），面向电信运营商分类的 `recurring` 订阅；表单入口只在电信运营商分类显示。该字段只控制提醒 / 续费弹窗 / 卡片等文案切换，不改变计费、续费推进、报表统计逻辑。
- `subscriptions.is_paused` 暂停标记，与 `is_active` 正交：`is_active=false` 表示停用归档，`is_paused=true` 表示临时暂停计费。暂停订阅不计支出（dashboard/reports）、不提醒（scheduler）、不进日历与雷达（recurrence/radarBucket），但账本页可见可恢复；`GET /api/subscriptions?active=true` 的「生效中」语义为 `is_active=true AND is_paused=false`。
- `subscriptions.remind_days_before` 保存每个订阅自己的提醒天数（如 `7,1`）。非负值表示提前 N 天 / 当天提醒；负值表示过期后第 N 天补提醒（如 `-1,-7`），扫描按 `days_left == n` 严格匹配；可靠队列用 `(subscription_id, business_date, days_before, channel)` 唯一键幂等去重。
- `bundles` 用于套餐包 / 组合订阅管理，订阅通过 `bundle_id` 归属。
- `family_members` 是 JSON 数组，用于家庭共享成员展示。
- `notification_outbox` 保存不含通道凭据的安全投递快照，状态仅允许 `pending / sending / retry_wait / sent / dead / canceled`；Token、Device Key、Webhook URL/secret 始终在发送前从当前用户配置读取。`scheduler_state` 独立记录最后完成扫描的业务日期，不能用“当天是否有任务”推断空扫描已完成。
- `notification_log` 只记录真实外发尝试；`channel` 区分 `telegram`、`bark` 与 `webhook`，`outbox_id / attempt_no` 把每次尝试关联回可靠队列。旧日志保持可读，但不会被回填成可重发任务。
- `renewal_history` 每次标记续费 append 一条，记录当时金额/币种/mode（today|due）/前后到期日快照，供详情页回看完整续费轨迹；随订阅 JSON 备份/恢复（按订阅下标映射到新订阅 ID），删除订阅/用户时同步清理，避免孤儿与 ID 复用导致历史错挂。
- `activity_log` 支撑网页实时日志；普通用户看自己的记录，管理员可看全站记录。
- `refresh_sessions` 以 JWT `jti` 为主键保存可用 Refresh Session 与过期时间；刷新时原 session 通过原子删除一次性消费并创建新 session，logout 删除当前 session。旧版无 `jti` 的 Refresh Token 以 SHA-256 指纹记录一次性迁移占用，防重复重放；多设备 session 相互独立。
- `credit_cards` 独立于订阅与付款方式，仅登记展示与计划数据：`display_name / bank_name / last_four`（可选 4 位尾号）、`statement_day / due_day`（名义日，月末锚定）、`remind_days_before`（JSON 整数数组，0–30、去重降序、空数组=不外发）、可选 `credit_limit`（授信上限，仅页面展示与备份往返）、`is_active / show_in_calendar` 与可选 `repaid_through_due`（已还到的名义还款日界线，标记还款时推进、取消不回拨）。绝不存储卡号、CVV、有效期或网银凭据；提交含疑似完整卡号的名称返回 422。`credit_card_notification_outbox` / `credit_card_notification_log` 与订阅 Outbox 结构对称、以 `credit_card_id` 关联，唯一键 `(credit_card_id, due_date, days_before, channel)` 保证幂等；删除单卡/用户时按 Log → Outbox → Card 显式清理。
- `calendar_feed_tokens` 每用户最多一条，原始 Token 不落库；`uid_namespace` 在首次生成时随机创建并在重置/撤销后保持，用作长期稳定的 iCal UID 命名空间；`revoked_at` 为空表示启用，重置只替换 hash，删除用户时显式清理。该表及 Feed URL 不进入单用户或整站 JSON 备份。

---

## 6. 核心功能模块

1. **鉴权与用户管理**
   - 注册 / 登录 / Refresh Token：Access Token 只驻留浏览器内存并随 API 请求放入 Bearer Header；Refresh Token 使用 `Path=/api/auth` 的 HttpOnly Cookie。每枚 token 对应服务端 `refresh_sessions.jti`，刷新时一次性消费旧 session 并签发新 session，退出时撤销当前 session 后删除 Cookie；旧 token 重放返回 401。
   - 为兼容已打开的旧前端，后端暂时同时接受 body `refresh_token` 并在 `TokenOut` 返回该字段；新前端只在首次加载时用旧 localStorage Refresh Token 迁移一次，成功后删除两个旧键。该兼容桥待至少一个发布版本后单独移除。
   - SMTP 邮箱验证码（可选）。
   - 管理员审核、启停账号、管理员权限分配、密码重置、删除用户。
   - 管理员数据诊断：检查通知配置缺口、订阅脏数据、引用越权、保号范围异常、一次性买断残留周期字段、孤儿通知日志、近期失败通知，以及 Outbox dead-letter、重试积压、过期租约和孤儿任务。诊断项 code 包括：`telegram_config_incomplete`、`bark_config_incomplete`、`webhook_config_incomplete`、`disabled_user_has_active_subscriptions`、`invalid_billing_type`、`invalid_cycle`、`invalid_cycle_count`、`negative_amount`、`currency_missing`、`category_missing` / `category_not_owned`、`payment_method_missing` / `payment_method_not_owned`、`bundle_missing` / `bundle_not_owned`、`subscription_missing_next_renewal`、`invalid_remind_days`、`keepalive_scope_invalid`、`one_time_has_recurring_fields`、`notification_user_missing`、`notification_subscription_missing`、`recent_notification_failures`、`notification_dead_letters`、`notification_retry_backlog`、`notification_lease_timeout`、`notification_outbox_orphan`。其中「引用缺失 / 越权、保号越界、一次性买断残留、无效提醒天数、周期订阅缺续费日」支持一键修复（`POST /api/admin/diagnostics/repair`，仅管理员）：修复前重判该问题仍存在、修复后复核引用合法性，需人工判断的项仍只报告。
   - 用户数据按 `user_id` 隔离。

2. **订阅管理**
   - 周期订阅 / 一次性买断。
   - 支持名称 / 套餐 / 备注客户端搜索、类型与续费风险组合筛选；任一筛选生效时禁用拖拽排序，完整订阅快照和筛选结果分别表达真实空态与无匹配空态。
   - 支持套餐名、备注、URL、VPS IPv4 / IPv6、家庭成员、套餐包、日历显示开关、同分类排序与可选 `end_date`；家庭共享、捆绑包、附加信息与日历选项通过原生 `details` 渐进披露，编辑时默认展开。
   - `end_date` 是包含式最后有效续费日：截止日当天 occurrence 仍有效，之后不再进入当前月化成本、续费风险、未来预测或提醒扫描；订阅记录不自动归档。一次性买断会清空 `end_date`。
   - 续费日期计算：创建周期订阅未指定 `next_renewal_date` 时，从 `start_date` 推进到「今天及之后」的第一个到期日；`cycle_count` 小于 1 按 1 处理、最大允许 1000；非法 `cycle` 默认按月；月末日期会夹到目标月最后一天。历史越界周期在 Feed 生成时转换为受控错误，不击穿请求。
   - 支持续费后按当前日期重新计算，也支持按原到期日滚动。
   - 支持每订阅自定义 `remind_days_before`。
   - 删除订阅需提交当前用户密码校验。

3. **仪表盘、日历与报表**
   - 月化 / 年化成本、按月化成本分类与订阅排行、最近付款、即将续费、已过期、永久购买统计；月化规则由 `billing.monthly_cost()` 唯一定义，排行使用 `monthly_cost_in_base`，`amount_in_base` 始终只表示单次金额折合。
   - 财务汇总只累计严格换算成功的金额，并返回 `financial_completeness`（是否完整、计入/排除数量、缺失币种）；缺汇率的行保留原币金额但基准币折合字段为 `null`。
   - 趋势图可切换 3 / 6 / 12 个月；历史部分由 `RenewalHistory` 与一次性买断聚合，未来部分复用前端 recurrence 引擎，月份连续补零并在当前月堆叠两类金额；view model 同时生成金额轴、跨年标签、可见 Tooltip、键盘方向导航与等价数据表。
   - 日历按 `next_renewal_date` 展示订阅，受 `show_in_calendar` 与包含式 `end_date` 控制；私有 iCal Feed 在设置页生成，供系统/云日历订阅同一套 recurrence。
   - 生产构建注册 PWA Service Worker；只预缓存静态离线页与品牌图标，非 API navigation 使用 network-first 并仅在网络异常时回退。SPA shell、登录页、API、Feed 与用户数据不进入 Cache Storage。
   - 日期字符串按本地日期解析，避免 `YYYY-MM-DD` 被 UTC 偏移。

4. **分类、付款方式与套餐包**
   - 系统预置 + 用户自定义；设置页提供普通用户管理入口，系统项只读。报表按 `category_id` 聚合并返回 `category_color/category_icon`，同名不同 ID 不合并，越权历史引用落入未分类。
   - 删除自定义分类或付款方式时，在同一事务解除本人订阅引用；分类删除同时清理用户 `category_order`，避免 SQLite 未启用外键级联时留下悬空引用。
   - 付款方式种子按名称幂等补齐，不覆盖现有数据。
   - 支持分类排序与订阅同分类拖拽排序；套餐包用于把多个订阅归组展示。

5. **多货币**
   - 预置主流货币，支持用户自定义货币、显示符号与手动汇率。
   - 手动汇率 API 使用「1 自定义货币 = X 用户基准货币」口径，内部仍换算为系统汇率基准币到 quote 的报价；只接受有限正数，缺少基准报价时拒绝保存而非按 1:1 回退。
   - 自定义货币代码创建后不可修改；需存在可用手动汇率才能写入订阅或设为基准币，被货币所有者本人的基准币或订阅引用时禁止删除或清空汇率，未引用删除会同步清理关联 `exchange_rates`；历史跨用户非法引用由诊断暴露，但不能阻止所有者管理自己的货币。
   - 汇率缓存后按用户 `base_currency` 统计；JSON 备份保存内部系统基准报价，保证用户基准币本身为自定义货币时仍可精确恢复。

6. **通知提醒**
   - APScheduler 每天按 `REMINDER_SCAN_TIME` 扫描并入队；扫描与 `scheduler_state` checkpoint 在同一短事务提交，不执行外部 HTTP。启动后若本地当天尚未完成扫描，只补扫当天一次，避免历史通知风暴。
   - 每分钟维护任务先检查本地当天 checkpoint，未完成就幂等补扫，再运行 Dispatcher；因此恢复清空当天 Outbox、启动补扫短暂锁冲突或单次补偿失败都会在下一分钟继续恢复。Dispatcher 使用 compare-and-set 与 120 秒租约小批量认领，认领事务提交后才并发调用 Telegram、Bark 或 Webhook；过期 `sending` 租约可恢复。系统承诺 at-least-once，不承诺 exactly-once。
   - 网络异常、HTTP 408/425/429/5xx 进入 `retry_wait`，按 1 分钟、5 分钟、15 分钟、1 小时、6 小时退避；普通 4xx、配置不完整和确定性供应商拒绝进入 `dead`，最多 6 次真实尝试。发送前用户/订阅/续费日/通道状态变化会转 `canceled`。
   - Webhook 发送 UTF-8 JSON；以用户必填的共享密钥对原始请求体计算 HMAC-SHA256，写入 `X-Subly-Signature: sha256=<hex>`，并附稳定的 `X-Subly-Delivery-ID: subly-<随机 128-bit id>` 供接收端幂等。
   - 每次真实外发写入 `notification_log`；`retry_cycle + attempt_no` 区分手动恢复前后的轮次，API 按日志写入顺序展示真实审计时间线。通知中心展示 Outbox 六种状态、安全错误、下次重试和 attempt 历史，仅 `dead/retry_wait` 可由本人原子开启下一轮并重置为 `pending`，请求内不同步发送。
   - 管理员可运行提醒 dry-run 模拟：与生产扫描共享候选规划，区分 `would_send / already_queued / already_sent / dead_letter` 等状态；模拟只构造安全预览，不联网、不写库。

7. **内置服务管理与图标系统**
   - 内置服务库支持多分类、启停、软删除 / 恢复、排序与图标预热。
   - 前端统一使用 `ServiceIcon` 渲染订阅与服务图标。
   - 支持 Emoji、上传图片、外部 URL、内置服务 favicon 与 fallback。

8. **备份恢复**
   - 普通用户可导出 / 导入自己的 JSON 备份。
   - 管理员可导出 / 恢复整站数据。

9. **实时日志与容器日志**
   - `activity_log` 提供网页实时日志。
   - 后端 stdout 记录请求概要、异常与慢请求，方便 Docker / NAS 排障。

10. **核心页面数据可信状态**
   - 雷达总览、续费日历、订阅账本与支出报表区分首次加载、成功后的真实空数据、首次错误、后台刷新和刷新失败后的陈旧数据；首次请求失败不得用零值或空列表伪装成业务事实。
   - 主数据请求使用代际守卫，只允许最后开始的请求写入；筛选或 Tab 快速切换时，晚到的旧响应必须丢弃。
   - 分类、付款方式、币种、捆绑包和图标库属于辅助元数据，各自失败时保留已有值或降级显示，不阻断主列表、日历或报表数据。
   - 陈旧数据继续可见并带 `data-trust="stale"` 标记和重试入口；首次未知状态使用 `data-trust="unknown"`，供可访问提示与 Playwright 数据可信测试定位。

---

## 7. 提醒调度逻辑

```text
每天 REMINDER_SCAN_TIME / 启动当天补扫:
  短事务内规划所有命中的订阅 + 通道
  为每个候选构造不含凭据的安全 payload
  INSERT notification_outbox ... ON CONFLICT DO NOTHING
  UPSERT scheduler_state.last_completed_business_date
  COMMIT（扫描阶段到此结束，不联网）

每分钟维护任务:
  if 当天 checkpoint 未完成: 先再次幂等扫描入队
  短事务 CAS 认领 pending / 到期 retry_wait / 过期 sending
  写 status=sending、lease_token、lease_expires_at 后 COMMIT
  重新读取当前用户、订阅、续费日与通道状态
  若状态变化：canceled；若配置不完整：dead
  否则离开事务执行外部 HTTP
  新短事务写回 sent / retry_wait / dead，并 append notification_log attempt
```

补充规则：

- 一次性买断、停用、暂停、已超过包含式 `end_date` 或用户禁用的订阅不入队。
- 唯一键 `(subscription_id, business_date, days_before, channel)` 负责数据库级幂等；升级前已有同业务日本地成功日志也会阻止重复入队。删除订阅/用户与 `replace=true` 恢复会显式先清 `notification_log`、`notification_outbox` 和 `renewal_history`，防 SQLite ID 复用污染。
- Dispatcher 最多并发 4 个外部调用；租约 Token 防止旧 worker 在任务被重新认领后覆盖新结果。发送成功但进程在结果提交前崩溃时，租约过期后会再次投递，因此是 at-least-once。
- 自动退避为 1 分钟、5 分钟、15 分钟、1 小时、6 小时；第 6 次失败或永久失败进入 `dead`。错误只保存异常类型或 HTTP 状态码，不保存 URL、响应体、Token 或 secret。
- 用户可在网页里发送通道测试；管理员“扫描并入队”为全站操作，仅管理员可用，普通用户返回 403。手动扫描遇进程内扫描锁占用返回 409，但不会在 HTTP 请求里同步投递。

---

## 8. 图标显示逻辑

前端统一使用 `ServiceIcon`：

- `src` 是图片 URL 时优先显示图片。
- 图片加载失败时切到 fallback。
- 旧透明 1x1 占位图会通过 `naturalWidth <= 1 && naturalHeight <= 1` 识别并切到 fallback。
- fallback 优先使用 Emoji，否则按服务名 / 订阅名首字符生成稳定颜色的字母图标。

后端图标库路径保持 `/api/icons/library/{slug}.png` 兼容，但响应的真实 `Content-Type` 可以是 PNG / ICO / WEBP / JPEG / SVG。远端 SVG 只有通过消毒后才会缓存；失败时返回可见 SVG fallback。

---

## 9. 部署方案

### 9.1 Docker Hub / GHCR 镜像部署

```bash
docker run -d --name subly \
  -p 8842:8000 \
  -e JWT_SECRET="$(openssl rand -hex 32)" \
  -e ADMIN_USERNAME=admin \
  -e ADMIN_PASSWORD='please-change-this-admin-password' \
  -e ADMIN_EMAIL=admin@example.com \
  -e TZ=Asia/Shanghai \
  -v subly_data:/app/data \
  --restart unless-stopped \
  <你的用户名>/subly:latest
```

访问：`http://<host>:8842`；API 文档：`http://<host>:8842/docs`。

### 9.2 镜像 compose 部署

```bash
docker compose -f docker-compose.hub.yml up -d
```

`docker-compose.hub.yml` 默认映射 `8842:8000`，数据卷为 `subly_data:/app/data`。容器入口以 root 对 `/app/data` 执行兼容性 ownership 修复（按设备号过滤，不跨文件系统），随后通过 `gosu` 立即 `exec` 为固定 UID/GID `10001` 的 Uvicorn 进程；这样旧版镜像留下的 root-owned 命名卷或 bind mount 可直接升级。宿主文件系统必须允许 chown；容器必须以默认入口运行或显式 `user: "10001:10001"`，其他非 root UID 会在启动时被拒绝。

### 9.3 源码 compose 部署

```bash
cp .env.example .env
# 编辑 .env 后：
docker compose up -d --build
```

`docker-compose.yml` 会构建本地镜像，并通过 Caddy 暴露 `80/443`。前端阶段严格使用 `npm ci` 与 lockfile；`.dockerignore` 排除本地依赖、构建产物、数据库、备份和 `.env`，避免污染镜像构建上下文。

### 9.4 镜像发布

GitHub Actions `docker-publish.yml` 支持：

- PR、push / tag 与手动运行都先执行快速验证：Python 3.12 后端全量测试 + Ruff、Node 20 前端 `npm ci` / ESLint / Vitest / build 与两份 compose 配置校验。
- `pip-audit --strict` 与 `npm audit --audit-level=high` 是 `verify` 的阻断式依赖门禁：任何已知 Python 漏洞、依赖解析失败或 High/Critical npm 漏洞都会直接终止 PR 与发布链路。
- PR（包括外部 fork 或手工依赖升级）验证通过后即结束，不构建双架构镜像、不运行 Trivy / Playwright，也不登录或推送镜像，避免每个 PR 消耗完整发布矩阵。
- `main` push、`v*` tag 与从 `main` 发起的手动发布才分别构建 `linux/amd64`、`linux/arm64` Docker 归档并对确定产物执行 Trivy；任一架构存在修复版本的 High/Critical 漏洞都会阻断，同时按架构上传 SARIF。扫描通过后，对同一 amd64 归档启动临时容器并运行 Chromium Playwright 登录/刷新/迁移/退出/CORS/CSP smoke；全部通过才下载同一批归档、推送架构临时 tag 并组装 Docker Hub / GHCR 多架构 manifest，不再二次构建，避免“扫 A 发 B”。
- 推送到 `main` 或 `v*` tag 时自动构建并发布；手动执行 `workflow_dispatch` 时必须从 `main` 运行并填写版本号，其他分支会在验证阶段明确失败。`v*` tag 必须指向 `main` 历史中的提交，防止未合并代码绕过发布门禁。
- `main` push 与手动发布同时更新 Docker Hub / GHCR 的 `latest` 和分支名/指定版本 tag；`v*` tag 事件只发布对应版本 tag，不回写 `latest`，避免给旧提交补 tag 时回滚正式镜像。版本号按 Docker tag 规则校验。
- 为保持远端仓库只存在 `main` 分支，不配置 Dependabot 自动 version-update PR；pip、npm、GitHub Actions 与 Docker 基础镜像升级统一人工规划，语言依赖风险由阻断式 audit 在 `verify` 阶段处理，最终镜像与系统包风险继续由 Trivy 门禁处理。

---

## 10. 安全与隐私约束

- JWT 使用 PyJWT 签发与验证，算法允许列表在代码内固定为 HS256，不接受环境变量扩展；同一 `JWT_SECRET` 下兼容既有 python-jose HS256 Token，旧版无 `jti` Refresh Token 仍只允许一次性迁移。`JWT_SECRET` 必须使用至少 32 字符的随机强密钥；空值、已知占位值或过短密钥会在建库与启动调度器前拒绝启动。`ALLOW_INSECURE_DEFAULTS=true` 仅用于本地演示，公网禁止开启。
- 管理员初始密码仅在首次创建账号时读取，必须至少 12 位且非已知默认值；已有管理员不会因环境变量保留旧值而被重置或阻断升级。
- 登录、注册与邮箱验证码入口使用线程安全的进程内滑动窗口限流；适配当前单 worker 部署，进程重启会重置，多 worker / 多副本需迁移到共享存储或反代限流。限流客户端地址取 Uvicorn 处理后的 `request.client.host`；默认 Caddy compose 因 app 仅在容器内网暴露而设置 `FORWARDED_ALLOW_IPS=*`，自行直接暴露 app 端口时必须改为可信代理 IP/CIDR 或不设置。
- 登录、Refresh Token 与受保护 API 统一检查 `email_verified` / `is_approved` / `is_active`；管理员撤销审核或禁用账号后，现有 Access Token 与 Refresh Token 都会被拒绝。前端只有在服务端 logout 成功后才清本地会话；网络/5xx 时保留登录态并提示重试，避免 HttpOnly Cookie 未删除却制造“假退出”。
- 注册需要 SMTP 时先发送验证码再提交用户；发送失败返回泛化错误且不占用用户名/邮箱，底层异常仅记录错误类型。验证码过期的未验证账号可凭相同用户名、邮箱和原密码重新注册，复用原记录并签发新验证码，避免“请重新注册”却被唯一约束永久卡死。
- `.env`、数据库文件、备份文件、图标缓存、上传文件等本地数据不应提交到仓库。
- 请求日志当前记录 method、path、status、duration、user_id、client 与 request_id，用于排障。
- 请求日志不得记录 request body、headers、query、密码、JWT、Bot Token、Device Key、API key、SMTP 密码、完整外部 URL、notes、remark、family_members 等敏感内容。
- 图标下载错误只记录必要的事件、slug、provider 和错误类型。
- JSON 备份包含个人订阅、账户偏好等数据，但刻意不导出 Telegram Bot Token、Bark Device Key、Webhook secret、`notification_outbox`、`scheduler_state`、`notification_log`、`calendar_feed_tokens` 或 Feed URL；恢复后需重新配置通知通道和私有日历。整站备份仍包含密码哈希，导出后应妥善保管。完整数据卷备份会包含运行态队列与尝试审计，应按数据库敏感数据保护。
- 私有 iCal 原始 Token 只显示一次且不落库；重置/撤销立即失效，所有无效状态统一 404。Feed 与 Token 管理响应禁止缓存、索引和 referrer 泄漏，Uvicorn 默认 access log 关闭以避免 query Token 进入容器日志。
- PWA Service Worker 不缓存 SPA shell、API、Feed、登录页或用户数据；HTML 与 `/sw.js` 使用 `no-store/no-cache`，Manifest 使用 `no-cache`，只有 Vite 哈希 `/assets/` 使用长期 immutable。CSP 显式限制 `worker-src 'self'` 与 `manifest-src 'self'`。
- 生产环境建议放在 HTTPS 反向代理后，并限制管理端入口暴露范围。
- 生产前端由 FastAPI 同源托管，开发通过 Vite `/api` proxy 保持同源，因此不启用 wildcard CORS；任意外部 `Origin` 不会获得 `Access-Control-Allow-Origin` / credentials 许可。若未来出现明确的跨域浏览器客户端需求，再增加显式 allowlist，不恢复 `*`。
- HTML 响应统一增加 CSP 与 `X-Content-Type-Options: nosniff`、`Referrer-Policy`、`X-Frame-Options: DENY`、`Permissions-Policy`。SPA 的脚本仅允许 `'self'`，样式首版保留 `'unsafe-inline'` 兼容现有动态样式，图片允许同源/data/blob/http/https；`/docs`、`/redoc` 使用独立的 jsDelivr 兼容策略，不为文档页面全局放宽 SPA。
- 订阅 `url` 字段后端做协议白名单校验，仅允许 `http://` / `https://`，拒绝 `javascript:` / `data:` 等危险协议。
- 手动触发全站提醒扫描（`/api/notifications/run-scan`）仅管理员可用，普通用户返回 403；扫描只幂等入队，不在请求内真实外发通知。
- Telegram / Bark / Webhook 测试发送固定使用当前用户已存配置（Bot Token / Bark 服务器 / Webhook URL 与 secret 等），不接受请求 payload 覆盖出网目标与发送凭证，防止借后端做消息中继或 SSRF；外部失败只返回泛化错误，不回显 URL、secret 或底层异常。
- 用户级出网目标（`telegram_api_base` / `telegram_proxy` / `bark_server` / `webhook_url`）经 `validate_outbound_url` 校验：仅允许 `http/https`，禁止 query / fragment / userinfo（防止把 `/bot.../getMe` 拼进 query 绕过约束），拦截链路本地（含云元数据 `169.254.169.254`）与危险协议；本机 / 私网不拦（自托管本地代理属正当用途）。启动迁移会清理历史不合规值。
- `/api/icons/from-url`（从任意 URL 下载图标，存在可读型 SSRF 风险）仅管理员可用；普通用户仍可上传图片、用内置服务库或直接填写图标 URL。
- 图标上传（`/api/icons/upload`）与 `from-url` 的 SVG 必须经白名单消毒（`_sanitize_svg_icon`，fail-closed），防止同源 SVG 存储型 XSS；Refresh Token 已迁移到 HttpOnly Cookie，Access Token 只驻留当前页面内存，进一步降低任意前端 XSS 的长期凭据窃取面。
- 自定义服务 `domain` 写入前经 `_validate_icon_domain` 校验（接收原始输入，拒协议 / userinfo / 路径 / query / fragment，显式拒 `localhost`，字面 IP 与 DNS 解析后的地址都拦本机 / 私网 / 链路本地 / 元数据 / 非常规 IPv4 字面量）；favicon 与 `from-url` 抓取改为手动跟随重定向（`_safe_resolve`，`follow_redirects=False`），每一跳 host 经 `is_internal_host` 校验后才发请求，防「公网 302 跳内网」在复核前已发出。
- 备份导出只包含本人或系统级的分类 / 付款方式，不打包订阅越权引用到的他人私有元数据；自定义货币同时保存可精确恢复的系统基准报价。备份导入在任何删除 / 写入前先做全量校验（缺 `subscriptions`、缺 name、非法日期、结束日期早于开始日期、非法/非有限正数汇率、类型错误、辅助集合元素非对象均返回 400），整站恢复缺 `username` 的用户块报错而非静默跳过，避免「先清空再宽松导入」丢数据。
- 通知测试端点的外部调用失败统一返回泛化提示；Outbox 只保存异常类型或 HTTP 状态码，底层 URL、响应体、Token、Device Key 与 secret 不进入 payload、API、尝试日志或 ActivityLog。
- 信用卡通知与 iCal 只陈述计划日期并以银行账单为准，不含金额、额度、账务状态或尾号；外发标签经 `external_card_label` 净化（剥离名称中与尾号相同的 4 位数字序列），`credit_limit` 从不进入 payload / iCal / 尝试日志。信用卡事件 UID 使用独立 `subly-credit-card-` 前缀。
- 删除订阅需提交当前用户密码并通过校验，防止误删与他人操作。
- 订阅引用的分类 / 付款方式 / 套餐包做归属校验：仅允许系统级或本人账户下的引用，跨用户引用在创建 / 更新时被拒，历史脏数据由管理员诊断报告。

---

## 11. 当前演进方向

Subly 当前围绕“自托管续费雷达”持续收口：

- 将部署依赖聚焦到内置 SQLite，降低 NAS 和个人服务器维护成本。
- 保持中文单语言，减少后续开发维护负担。
- 提供 Telegram + Bark + Webhook 多通道提醒，并支持 Bark 可选 TTL 与 Webhook HMAC-SHA256 签名 JSON。
- 强化内置服务库、多分类、图标下载、SVG 消毒缓存与可见 fallback。
- 完善注册审核、用户管理、备份恢复、实时日志和文档一致性。
